# G_QMark Visual Design Specification

这份文件记录 `g-qmark-visual` 的稳定身份规则。`SKILL.md` 负责告诉 Agent 如何生产视觉内容；这里负责定义哪些视觉特征不应该在不同生成任务中随意变化。

## 1. 整体风格

六个关键词：

- 明亮
- 安静
- 略困
- 松弛
- 圆润
- 技术内容清楚

视觉目标不是“科技感很强”，而是让技术内容出现在一个干净、温和、容易阅读的编辑插画系统中。

默认画面使用暖米白背景、大面积留白、深灰线条和少量柔和色块。小火柴人和 Golden Pup 可以带来个性，但不能压过信息本身。

## 2. 标识系统

### 2.1 身份层级

- `G_QMark` 是正式项目与视觉系统名称，用于仓库、文档、标题页和完整署名。
- `G_Q` 是核心人格化短标，用于头像、视频角标、水印和片头。
- `G?` 是次级抽象符号，可用于 favicon、微型图标和衍生动效，不再是唯一主标识。

`G_Q` 的第一联想应是一个正在轻轻揉惺忪睡眼的极简表情：`G` 与 `Q` 的圆形结构像两只困倦的眼睛或揉眼的手，`_` 像平静的嘴部或面部中线。

- 主体默认使用 `#30343B`。
- 保留横向字形与下划线，不做字面化的手、睫毛或卡通脸。
- 可以略微强化圆形结构的“眼睛感”，但必须首先保持 `G_Q` 可读。
- 整体优先传达安静、略困、松弛、克制、已经开始工作的状态。
- 正式 v1 SVG 已存放在 `assets/identity/`；不得用临时生成图替换其几何结构。

### 2.2 黄色圆点

黄色圆点是比完整 Logo 更常用的身份元素。

可以用于：

- 问号的点
- 章节编号
- 当前流程节点
- 小型状态点
- 光标或聚焦提示

不能用于：

- 大面积背景
- 大块按钮或卡片填色
- 每个对象都加黄点
- 与信息含义无关的装饰散点

## 3. 配色

| Token | HEX | 默认用途 |
| --- | --- | --- |
| `canvas-warm` | `#FFFDF7` | 主背景 |
| `surface-white` | `#FFFFFF` | 卡片、纸张、设备亮面 |
| `ink-primary` | `#30343B` | 文字、主轮廓 |
| `ink-secondary` | `#60656F` | 次级轮廓、辅助线 |
| `identity-yellow` | `#F2C94C` | 身份、聚焦、当前点 |
| `semantic-blue` | `#8EC5E8` | 输入、信息、数据、云 |
| `semantic-green` | `#A9C8A5` | 成功、稳定、完成 |
| `semantic-orange` | `#F2A66F` | 动作、警告、成本、摩擦 |
| `semantic-purple` | `#B9A7D8` | AI、Agent、模型、处理 |

一张普通解释图一般只使用 1–2 个语义色，加少量身份黄。不要为了“品牌统一”把整张图涂成黄色。

## 4. Wenhao 小火柴人

### 4.1 固定识别特征

- 稍大的简单圆头
- 仅用几笔表示的黑色短发
- 极简眼镜
- 微困、安静的点状或短线眼睛
- 很短的线条身体和四肢，总高约 2.5–3 个头
- 小块深色上衣

不要画成高挑、长腿或写实比例的火柴人，也不要回到完整卡通人物。

### 4.2 角色定位

Wenhao 代表内容中的“我”或讲述者，可用于思考、测试、拍摄、阅读、DIY、演示和操作设备。

动作必须保持简笔化：小手只需表达方向和持物关系，不画手指、衣褶或鞋带。

## 5. Golden Pup 金毛幼犬

### 5.1 固定识别特征

- 明确的金毛幼犬，而不是成年犬或通用小狗
- 头身几乎连成一个低矮豆子形，没有明显脖子
- 两侧是宽而软的半圆形垂耳
- 手脚只用很短的圆弧表示，尾巴小而卷
- 点状眼睛、小椭圆鼻子和放松的小嘴
- 奶油色小嘴部色块与暖金黄色平涂身体
- 一条细的暖红色项圈，不加吊牌、铃铛或蝴蝶结
- 与小火柴人一致的粗圆深灰简笔线条

### 5.2 角色含义

Golden Pup 可以代表陪伴、好奇、等待、观察、轻微疑惑、进度和完成状态。它不是机器人助手，也不需要每张图都出现。

### 5.3 常用动作

- 坐在小火柴人身边
- 下巴趴在前爪上
- 跟随或观察
- 叼着软球
- 从文件夹后探头
- 趴在进度条、电脑或 NAS 旁
- 在相机、3D 打印机或电子项目旁等待

避免分离明显的写实头身结构、写实毛发、尖猫耳、边牧花色、成年犬比例、大眼动漫表情、复杂服装和机械部件。

## 6. 图形语言

### 6.1 线条

- 优先深灰而非纯黑。
- 主轮廓清楚，转角圆润。
- 不追求钢笔般完全机械，也不追求抖动明显的草图感。
- 箭头可以轻微弯曲，但整体路径必须清楚。

### 6.2 形状

常用：

- 圆角矩形
- 圆点
- 文件夹
- 纸张
- 简化窗口
- 模型块
- 芯片 / 设备盒子
- 流程线
- 简洁进度条
- 标签贴纸

不用默认加入：

- 发光电路纹
- 六边形科技底纹
- HUD 圆环
- 漂浮粒子
- 网格地平线

### 6.3 阴影和材质

- 以扁平色块为主。
- 允许非常轻的纸张质感。
- 阴影只用于分层，不作为视觉主角。
- 禁止玻璃拟态、镜面反射、金属高光和强渐变。

## 7. 技术对象词典

以后应逐步形成一套统一 SVG / PNG 图标，包括：

- Mac / laptop
- Mac Studio 类主机
- NAS
- local model
- cloud model
- Agent
- subagent
- document / folder
- camera
- microphone / audio
- 3D printer
- PCB / ESP32
- network node
- terminal
- progress / task state

图标应简化真实物体，但保留足够的识别特征。不要用抽象“AI 大脑”代替具体模型和 Agent。

## 8. 视频使用规则

视频中优先把不同资产拆开：

- `G_Q` 核心短标（必要时使用次级 `G?`）
- 黄色圆点
- Wenhao 小火柴人
- Golden Pup
- 设备图标
- 流程线
- 标题和字幕

这样可以在剪辑、HTML/CSS/GSAP 或其他动画工具里做确定性动画，而不是每次把整张画面重新生成。

推荐动作：

- 淡入 / 淡出
- 小幅滑入
- 轻微弹性出现
- 路径线绘制
- 进度条移动
- Golden Pup 尾巴轻摆
- `? → !` 的状态变化
- 黄色圆点沿路径移动

避免高频抖动、镜头乱飞、大量粒子或连续夸张缩放。

## 9. 尚未定稿的内容

以下内容当前只有方向，没有正式母版，生成工具不得把一次临时输出视为最终标准：

- 次级 `G?` 的缩减规则
- Wenhao 小火柴人的正式姿态母版
- Golden Pup 的正式比例、垂耳形状和金色范围
- 图标线宽和统一圆角数值
- Logo reveal 的后续音效与剪辑节奏
- 字体家族

这些内容应在后续选定母资产后再写入本文件。
