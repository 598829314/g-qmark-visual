# G_QMark Visual Design Specification

这份文件记录 `g-qmark-visual` 的稳定身份规则。`SKILL.md` 负责告诉 Agent 如何生产视觉内容；这里负责定义哪些视觉特征不应该在不同生成任务中随意变化。

## 1. 整体风格

五个关键词：

- 明亮
- 平稳
- 圆润
- 克制地可爱
- 技术内容清楚

视觉目标不是“科技感很强”，而是让技术内容出现在一个干净、温和、容易阅读的编辑插画系统中。

默认画面使用暖米白背景、大面积留白、深灰线条和少量柔和色块。人物和 Q Cat 可以带来个性，但不能压过信息本身。

## 2. 标识系统

### 2.1 主标识

主标识概念：`G?`

- `G` 与问号形成一个可独立使用的小型符号。
- 主体使用 `#30343B`。
- 问号的圆点固定使用 `#F2C94C`。
- 最终 SVG 形态尚未定稿，因此当前阶段不得自行发明复杂几何结构并当作正式 Logo。

### 2.2 黄色圆点

黄色圆点是比完整 Logo 更常用的身份元素。

可以用于：

- 问号的点
- Wenhao 的小吊坠
- Q Cat 的黄色小圆牌或固定黄色细节
- 章节编号
- 当前流程节点
- 小型状态点
- 光标或聚焦提示

不能用于：

- 大面积背景
- 大块按钮或卡片填色
- 每个对象都加黄点
- 与信息含义无关的装饰散点

## 3. 配色

| Token | HEX | 默认用途 |
| --- | --- | --- |
| `canvas-warm` | `#FFFDF7` | 主背景 |
| `surface-white` | `#FFFFFF` | 卡片、纸张、设备亮面 |
| `ink-primary` | `#30343B` | 文字、主轮廓 |
| `ink-secondary` | `#60656F` | 次级轮廓、辅助线 |
| `identity-yellow` | `#F2C94C` | 身份、聚焦、当前点 |
| `semantic-blue` | `#8EC5E8` | 输入、信息、数据、云 |
| `semantic-green` | `#A9C8A5` | 成功、稳定、完成 |
| `semantic-orange` | `#F2A66F` | 动作、警告、成本、摩擦 |
| `semantic-purple` | `#B9A7D8` | AI、Agent、模型、处理 |

一张普通解释图一般只使用 1–2 个语义色，加少量身份黄。不要为了“品牌统一”把整张图涂成黄色。

## 4. Wenhao Character

### 4.1 固定识别特征

- 黑色短发
- 简洁眼镜
- 深灰或黑色上衣
- 小型金黄色吊坠
- 轻度卡通化，整体约 1:3.5–1:4 的头身比例
- 五官简化
- 表情克制

### 4.2 角色定位

Wenhao 代表内容中的“我”或讲述者，用于：

- 思考
- 测试模型
- 查看数据
- 使用电脑
- 拍摄
- 阅读或检查材料
- DIY / 装配
- 演示一个结论

Wenhao 不等于“技术宅刻板形象”。不要默认增加连帽衫、耳机、机械义肢、霓虹眼镜、黑客符号等元素。

### 4.3 表情和动作

允许：

- 平静
- 专注
- 轻微疑惑
- 得到答案后的轻微确认
- 观察
- 指向
- 展示
- 操作设备

避免：

- 极端震惊
- 大哭大笑
- 表情包式夸张动作
- 超级英雄姿势
- 日漫式战斗表情

## 5. Q Cat

### 5.1 固定识别特征

- 奶灰色身体
- 暖白色肚皮或嘴部浅色区域
- 深灰线稿
- 尾巴自然卷成明显但不生硬的 `?`
- 一个固定黄色小细节：优先考虑小圆牌；正式角色稿确定后再锁定具体位置
- 简洁圆润的身体比例

### 5.2 角色含义

Q Cat 可以代表：

- 问题
- 好奇
- AI
- Agent
- 正在处理
- 等待
- 观察
- 完成状态

它不是每张图必须出现的吉祥物。如果加入以后不能提供信息或身份价值，就不加入。

### 5.3 常用动作

- 趴在进度条上
- 坐在 Mac / NAS / 模型块旁边
- 从文件夹或窗口后探头
- 看着 Agent 节点工作
- 抱着一个小模型块
- 在 3D 打印机旁观察
- 趴在相机包或设备箱上
- 在错误提示旁边轻微疑惑

避免：

- 机械猫
- 盔甲
- 赛博义眼
- 高写实毛发
- 复杂服装
- 过多拟人化动作

## 6. 图形语言

### 6.1 线条

- 优先深灰而非纯黑。
- 主轮廓清楚，转角圆润。
- 不追求钢笔般完全机械，也不追求抖动明显的草图感。
- 箭头可以轻微弯曲，但整体路径必须清楚。

### 6.2 形状

常用：

- 圆角矩形
- 圆点
- 文件夹
- 纸张
- 简化窗口
- 模型块
- 芯片 / 设备盒子
- 流程线
- 简洁进度条
- 标签贴纸

不用默认加入：

- 发光电路纹
- 六边形科技底纹
- HUD 圆环
- 漂浮粒子
- 网格地平线

### 6.3 阴影和材质

- 以扁平色块为主。
- 允许非常轻的纸张质感。
- 阴影只用于分层，不作为视觉主角。
- 禁止玻璃拟态、镜面反射、金属高光和强渐变。

## 7. 技术对象词典

以后应逐步形成一套统一 SVG / PNG 图标，包括：

- Mac / laptop
- Mac Studio 类主机
- NAS
- local model
- cloud model
- Agent
- subagent
- document / folder
- camera
- microphone / audio
- 3D printer
- PCB / ESP32
- network node
- terminal
- progress / task state

图标应简化真实物体，但保留足够的识别特征。不要用抽象“AI 大脑”代替具体模型和 Agent。

## 8. 视频使用规则

视频中优先把不同资产拆开：

- `G?` Logo
- 黄色圆点
- Wenhao
- Q Cat
- 设备图标
- 流程线
- 标题和字幕

这样可以在剪辑、HTML/CSS/GSAP 或其他动画工具里做确定性动画，而不是每次把整张画面重新生成。

推荐动作：

- 淡入 / 淡出
- 小幅滑入
- 轻微弹性出现
- 路径线绘制
- 进度条移动
- Q Cat 尾巴轻摆
- `? → !` 的状态变化
- 黄色圆点沿路径移动

避免高频抖动、镜头乱飞、大量粒子或连续夸张缩放。

## 9. 尚未定稿的内容

以下内容当前只有方向，没有正式母版，生成工具不得把一次临时输出视为最终标准：

- `G?` 的正式 SVG 几何形态
- Wenhao 的正面 / 侧面 / 三视图母版
- Q Cat 的正式比例、耳朵形状和黄色识别点最终位置
- 图标线宽和统一圆角数值
- Logo reveal 的最终动画时长和曲线
- 字体家族

这些内容应在后续选定母资产后再写入本文件。
